import { CheckCircle2, Download, Eye, FilePlus2, LayoutList, Plus } from 'lucide-react'
import Button from '../common/Button'
const metrics = [['Words', 'words'], ['Sections', 'sections'], ['Figures', 'figures'], ['Tables', 'tables']]
export default function ToolsPanel({ paper, statistics, onAddSection, onAddElement, onPreview, onExport, isExporting }) { const checks = [['Title present', Boolean(paper.title.trim())], ['Abstract present', Boolean(paper.abstract.trim())], ['Keywords present', paper.keywords.length > 0], ['Paper structure valid', paper.sections.length >= 5]]; return <aside className="tools-panel" aria-label="Paper tools"><section className="tools-section"><p className="tools-label">Paper statistics</p><div className="statistics-grid">{metrics.map(([label, key]) => <div className="stat" key={key}><span>{label}</span><strong>{statistics[key].toLocaleString()}</strong></div>)}</div></section><section className="tools-section"><p className="tools-label">Formatting status</p><div className="format-list">{checks.map(([label, completed]) => <div className={`format-check ${completed ? 'is-complete' : ''}`} key={label}><CheckCircle2 size={16} aria-hidden="true" /><span>{label}</span></div>)}</div></section><section className="tools-section quick-actions"><p className="tools-label">Quick actions</p><Button icon={Plus} onClick={onAddSection}>Add section</Button><Button icon={LayoutList} onClick={onAddElement}>Add paragraph</Button><Button icon={Eye} onClick={onPreview}>Preview</Button><Button icon={Download} onClick={onExport} disabled={isExporting}>{isExporting ? 'Exporting…' : 'Export PDF'}</Button></section><p className="tool-note"><FilePlus2 size={14} aria-hidden="true" /> Editing in <strong>{paper.template}</strong> mode</p></aside> }
