import { ChevronRight, FilePlus2, FileText, Plus } from 'lucide-react'
import Button from '../common/Button'
import { sectionHeading } from '../../utils/sectionLabels'
function OutlineItem({ section, path, selectedId, onSelect }) { const isSelected = selectedId === section.id; return <><button type="button" className={`outline-item ${isSelected ? 'is-selected' : ''} ${path.length > 1 ? 'is-child' : ''}`} onClick={() => onSelect(section.id)}>{path.length === 1 ? <ChevronRight size={14} aria-hidden="true" /> : <span className="outline-indent" />}<span>{sectionHeading(section, path)}</span></button>{section.children?.map((child, index) => <OutlineItem key={child.id} section={child} path={[...path, index]} selectedId={selectedId} onSelect={onSelect} />)}</> }
export default function Sidebar({ paper, selectedId, onSelect, onAddSection }) { return <aside className="sidebar" aria-label="Paper outline"><div className="sidebar-heading"><span>Paper outline</span><FileText size={16} aria-hidden="true" /></div><div className="template-indicator"><FilePlus2 size={14} aria-hidden="true" /> IEEE conference paper</div><nav className="outline-nav" aria-label="Paper sections"><button type="button" className={`outline-item outline-item--header ${selectedId === 'paper-header' ? 'is-selected' : ''}`} onClick={() => onSelect('paper-header')}><ChevronRight size={14} aria-hidden="true" /><span>Paper details</span></button>{paper.sections.map((section, index) => <OutlineItem key={section.id} section={section} path={[index]} selectedId={selectedId} onSelect={onSelect} />)}<button type="button" className={`outline-item outline-item--references ${selectedId === 'references' ? 'is-selected' : ''}`} onClick={() => onSelect('references')}><ChevronRight size={14} aria-hidden="true" /><span>References</span></button></nav><Button icon={Plus} className="add-section-button" onClick={onAddSection}>Add section</Button></aside> }
