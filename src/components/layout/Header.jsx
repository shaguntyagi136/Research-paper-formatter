import { Check, ChevronDown, Download, Eye, FileText } from 'lucide-react'
import Button from '../common/Button'
export default function Header({ template, saveState, onPreview, onExport, isExporting }) { return <header className="top-header"><div className="brand-group"><div className="brand-mark"><FileText size={18} aria-hidden="true" /></div><div><p className="product-name">ResearchPaper Builder</p><button type="button" className="template-selector" aria-label={`Current template: ${template}`}>{template} <ChevronDown size={13} aria-hidden="true" /></button></div></div><div className="header-actions"><span className="save-status"><Check size={15} aria-hidden="true" /> {saveState}</span><Button icon={Eye} className="desktop-action" onClick={onPreview}>Preview</Button><Button icon={Download} variant="primary" onClick={onExport} disabled={isExporting}>{isExporting ? 'Exporting…' : 'Export PDF'}</Button></div></header> }
