import EditableContent from './EditableContent'
const updateListItem = (items, index, value) => items.map((item, currentIndex) => currentIndex === index ? value : item)
export default function PaperHeader({ paper, onUpdatePaper, selected }) { const author = paper.authors[0] ?? ''; const affiliation = paper.affiliations[0] ?? ''; const keywords = paper.keywords.join(', '); const abstractLayout = paper.abstractLayout ?? 'full-width'; return <section id="paper-header" className={`paper-header-card ${selected ? 'is-selected' : ''}`}><div className="paper-header-topline"><span>Paper details</span><span>IEEE template</span></div><label className="title-field"><span className="sr-only">Paper title</span><input value={paper.title} onChange={(event) => onUpdatePaper('title', event.target.value)} placeholder="Enter your research paper title" /></label><div className="metadata-grid"><label className="metadata-field"><span>Authors</span><input value={author} onChange={(event) => onUpdatePaper('authors', updateListItem(paper.authors, 0, event.target.value))} placeholder="Name, co-authors" /></label><label className="metadata-field"><span>Affiliation</span><input value={affiliation} onChange={(event) => onUpdatePaper('affiliations', updateListItem(paper.affiliations, 0, event.target.value))} placeholder="Department, institution" /></label></div><div className="abstract-layout-setting"><span>Abstract layout</span><div role="group" aria-label="Abstract layout"><button type="button" className={abstractLayout === 'full-width' ? 'is-active' : ''} onClick={() => onUpdatePaper('abstractLayout', 'full-width')}>Full width</button><button type="button" className={abstractLayout === 'two-columns' ? 'is-active' : ''} onClick={() => onUpdatePaper('abstractLayout', 'two-columns')}>Two columns</button></div></div><EditableContent label="Abstract" value={paper.abstract} onChange={(value) => onUpdatePaper('abstract', value)} placeholder="Write a concise summary of your research, methods, findings, and contribution." className="abstract-field" /><label className="keywords-field"><span>Keywords</span><input value={keywords} onChange={(event) => onUpdatePaper('keywords', event.target.value.split(',').map((keyword) => keyword.trim()).filter(Boolean))} placeholder="e.g. research, methodology, analysis" /></label></section> }
