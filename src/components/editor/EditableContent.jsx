import { useEffect, useRef } from 'react'
export default function EditableContent({ value, onChange, placeholder, label, className = '' }) { const ref = useRef(null); useEffect(() => { if (ref.current) { ref.current.style.height = 'auto'; ref.current.style.height = `${Math.max(72, ref.current.scrollHeight)}px` } }, [value]); return <label className={`editable-field ${className}`}>{label && <span className="field-label">{label}</span>}<textarea ref={ref} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} aria-label={label ?? placeholder} /></label> }
